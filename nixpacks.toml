[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r app/requirements.txt"
]

[phases.build]
cmds = [
  "echo 'Build complete'"
]

[start]
cmd = "export PYTHONPATH=/app && python -m uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
