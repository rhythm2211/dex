# Production requirements - optimized for Railway deployment
# This file excludes testing and development dependencies

# --- Core Backend & API ---
fastapi
uvicorn[standard]
pydantic-settings
python-multipart
email-validator
requests>=2.31.0
httpx>=0.27.0
isodate==0.6.1  # Pinned to resolve dbt-common conflict

# --- AI Orchestration (LangChain) ---
langchain
langchain-community
langchain-groq
langchain-huggingface
langchain-voyageai  # Voyage AI embeddings (1024 dimensions)

# --- Vector Database (PostgreSQL + pgvector) ---
# Note: pgvector PostgreSQL extension must be installed in the database: CREATE EXTENSION vector;
# The pgvector Python package is required by LangChain's PGVector implementation
pgvector  # Python package for pgvector support in LangChain
psycopg[binary]  # For pgvector (LangChain)
psycopg2-binary  # For SQLAlchemy (user storage)

# --- Machine Learning & Embeddings ---
# Note: torch==2.10.0+cpu is installed separately in Dockerfile to use CPU-only version
# This saves ~3GB by excluding CUDA libraries (not needed on Railway CPU instances)
sentence-transformers
tokenizers==0.20.3

# --- Data Ingestion & Parsing ---
gitpython
tree-sitter
tree-sitter-languages

# --- Utilities ---
python-dotenv
tqdm
sqlalchemy
bcrypt
resend>=2.0.0

# --- Graph & ML Libraries ---
networkx 
scikit-learn
neo4j
pyyaml>=6.0.1
celery[redis]>=5.3.0
redis>=5.0.0
