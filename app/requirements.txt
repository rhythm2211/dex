# --- Core Backend & API ---
fastapi
uvicorn[standard]
pydantic-settings
python-multipart
email-validator
requests>=2.31.0
httpx>=0.27.0
isodate==0.6.1  # Pinned to resolve dbt-common conflict

# --- AI Orchestration (LangChain) ---
chardet  # optional encoding detection for TextLoader autodetect_encoding
langchain
langchain-community
langchain-groq
langchain-huggingface
langchain-voyageai  # Voyage AI embeddings (1024 dimensions)

# --- Vector Database (PostgreSQL + pgvector) ---
# Note: pgvector PostgreSQL extension must be installed in the database: CREATE EXTENSION vector;
# The pgvector Python package is required by LangChain's PGVector implementation
pgvector  # Python package for pgvector support in LangChain
psycopg[binary]  # For pgvector (LangChain)
psycopg2-binary  # For SQLAlchemy (user storage)

# --- Machine Learning & Embeddings ---
# Use CPU-only PyTorch to reduce image size (no CUDA libraries)
# Install torch CPU version first, then sentence-transformers will use it
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
sentence-transformers
tokenizers==0.20.3

# --- Data Ingestion & Parsing ---
gitpython
tree-sitter>=0.25.0
tree-sitter-javascript
tree-sitter-typescript
tree-sitter-java
tree-sitter-go
tree-sitter-rust
tree-sitter-ruby

# --- Utilities ---
python-dotenv
tqdm
sqlalchemy
bcrypt
resend>=2.0.0


networkx 
scikit-learn
neo4j
pyyaml>=6.0.1
celery[redis]>=5.3.0
redis>=5.0.0

# Testing (optional, for CI/CD)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
flake8>=7.3.0
black>=23.9.0
isort>=5.12.0
mypy>=1.5.0